# HITable 1.0.0 内部分发说明

## 安装

### 设备要求

- Android 8.0（API 26）及以上
- **64 位 ARM 设备**（2017 年后的绝大多数手机/平板；32 位老设备不支持）

### 安装步骤

1. 把 `HITable-1.0.0-arm64.apk` 传到手机
2. 点击安装，系统可能提示「未知来源应用」→ 允许
3. 如果之前装过同名应用且签名不同，需先卸载旧版

---

## 首次使用（3 步）

### 1. 设置学期

打开 App，第一周周一**已经默认填好**（2026 年 8 月 31 日），
总周数和每天节次也有默认值 —— **通常直接点保存即可**。

> 如果你所在学期的开学日期不同，再手动改。

### 2. 导入课表

**必须在校园网内**，然后：

菜单 →「从哈工大教务导入」→ 在页面里登录统一身份认证 → 点「读取课表」→ 预览确认 → 导入

- 登录在页面内完成，**应用不接触也不保存你的密码**
- 顶部可切换学期，一次读取任意学期
- 预览里黄色单元格可点击修改，误识别的行可删除

### 3. 允许通知（可选）

菜单 →「提醒设置」→ 允许通知权限。
**国产系统还需按页面内的引导关闭电池优化**，否则锁屏后提醒可能延迟。

---

## 功能一览

### 课表

- **今日 / 本周** 左右滑动切换
- 点日期行的「第几周」可跳转任意教学周
- 点左右箭头逐日切换，点日期回到今天
- 支持单双周、不连续周次、一门课多个时间段
- 长按课程卡片可修改、可设置单日临时停课

### 提醒

- 提前时间 1~1440 分钟可调
- 三档：仅通知 / 通知加震动 / 通知加震动和声音
- 与其它课程时间冲突时自动延后

### 桌面小组件

长按桌面空白处 → 小组件 → 找到 **HITable** → 添加「下一节课」

### 界面美化（菜单 →「界面美化」）

- 6 套配色 + 自定义主色/强调色
- 深浅模式、AMOLED 纯黑
- 卡片圆角 4 档、紧凑度 3 档
- 卡片样式 3 种（纯色/色条/渐变）+ 不透明度可调
- **主页背景**：预设图案，或选自己的图片（可调模糊度）
- **顶部问候语**：默认 `Hi，Table~`，可改任意文字、换字体（仿宋/楷体/黑体）、或隐藏

### 数据安全

- 完全离线，无账号、无云同步、无广告、无统计
- 菜单 →「备份与恢复」可导出 JSON 备份
- 换设备时用它迁移

---

## 常见问题

**Q：提示「不在校园网」/ 读取失败？**
A：教务系统只在校内网可访问。校外需先连学校 VPN，或改用手工添加课程。

**Q：提醒不准时？**
A：国产系统会限制后台。按「提醒设置」页里的引导，关闭电池优化、允许自启动和后台活动。

**Q：导入后课程有冲突？**
A：重叠的课程会照常导入，方便你自行取舍。长按卡片可删除不需要的。

**Q：想换界面风格？**
A：菜单 →「界面美化」，所有设置改完立即生效。

**Q：数据会丢吗？**
A：数据只存在本机。卸载前请先在「备份与恢复」里导出备份。

---

## 反馈

- QQ：647259208


> 非哈尔滨工业大学官方应用，请以学校教务通知和实际教学安排为准。
