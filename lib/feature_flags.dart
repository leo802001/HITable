/// 功能开关。
///
/// 集中管理「暂时下线」的功能：代码保留、入口隐藏，
/// 待适配完成把开关改回 `true` 即可恢复，不必翻找散落的 `if`。
library;

/// 是否启用「本地文件导入」（CSV / XLSX / XLS）。
///
/// **当前关闭**：学校教务导出的 `.xls` 是 OLE2 老格式，
/// 与网格式课表的行列映射尚未完全适配（实测有约一半课程未被识别），
/// 为避免同学导入后缺课，先把入口隐藏。
///
/// 仍然可用的导入方式：菜单里的「从哈工大教务导入」（校园网 + 统一身份认证）。
///
/// 待 `.xls` 网格式解析适配完成并经过真机验证后，改回 `true` 即可。
const kEnableLocalFileImport = false;
