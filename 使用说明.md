# HITable 1.2.0 使用说明

> 哈尔滨工业大学学生的本地优先课程表应用 · 非学校官方应用
> 请以学校教务通知和实际教学安排为准。

---

## 安装

### 设备要求

- Android 8.0（API 26）及以上
- **64 位 ARM 设备**（2017 年后的绝大多数手机 / 平板；32 位老设备不支持）

### 安装步骤

1. 把 `HITable-1.2.0-arm64.apk` 传到手机
2. 点击安装，系统可能提示「未知来源应用」→ 允许
3. 华为 / 荣耀设备会多两步：先点「继续安装」，再在应用市场页**勾选底部复选框**后点「继续安装」，最后按指纹或密码完成身份验证
4. 如果之前装过同名应用且签名不同，需先卸载旧版

---

## 首次使用（3 步）

### 1. 设置学期

打开 App，第一周周一**已经默认填好**（2026 年 8 月 31 日），
总周数和每天节次也有默认值 —— **通常直接点保存即可**。

> 如果你所在学期的开学日期不同，再手动改。

### 2. 导入课表

**必须在校园网内**，然后：

菜单 →「从哈工大教务导入」→ 在页面里登录统一身份认证 → 点「读取课表」→ 预览确认 → 导入

- 登录在页面内完成，**应用不接触也不保存你的密码**
- 顶部可切换学期，一次读取任意学期
- 预览里黄色单元格可点击修改，误识别的行可删除

### 3. 允许通知（可选）

菜单 →「提醒设置」→ 允许通知权限。
**国产系统还需按页面内的引导关闭电池优化**，否则锁屏后提醒可能延迟。

---

## 课表

- **今日 / 本周** 左右滑动切换
- **本周**支持两种排布，在「界面美化 → 课表排布」里切换：
  - **瀑布流**（默认）：按天分组纵向排列，逐条看课程详情
  - **表格**：横轴星期、纵轴节次，一眼看出整周空档
- 点日期行的「第几周」可跳转任意教学周
- 点左右箭头逐日切换，点日期回到今天
- 支持单双周、不连续周次、一门课多个时间段
- 长按课程卡片可修改、可设置单日临时停课

---

## 提醒

- 提前时间 1~1440 分钟可调
- 三档：仅通知 / 通知加震动 / 通知加震动和声音
- 与其它课程时间冲突时自动延后

---

## 桌面小组件（「今日课程」）

显示**当天全部课程**，每行一门（同一大节的课会合并成一行）：

- 左侧色条三态：**进行中=蓝 / 未开始=橙（显示开课时间）/ 已结束=灰**
- 已结束的自动沉到底部
- 顶部显示日期、教学周、当天节数
- 跟随系统深色模式自动切换

### 添加方式（各系统入口不同）

| 设备 | 添加入口 |
| --- | --- |
| 小米 / OPPO / vivo / 三星 | 长按桌面空白处 → 「小组件」→ 找到 **HITable** |
| 荣耀 / 华为（EMUI / 鸿蒙 4.x 及以下） | **双手捏合桌面 → 「服务卡片」→ 下滑找到「窗口小工具」→ 找到 HITable** |
| 华为纯血鸿蒙（HarmonyOS NEXT） | ⚠️ **加不了**，见下方说明 |

> 华为 / 荣耀的 Android 小组件**不在「服务卡片」主列表里**，而在该页**下滑后的「窗口小工具」**分类中 —— 这是最常见的「找不到」原因。

### ⚠️ 纯血鸿蒙（HarmonyOS NEXT）用户注意

纯血鸿蒙的桌面 **不挂载安卓小组件**（官方已知兼容性限制）。
App 本体可以通过「卓易通」正常安装使用，**但桌面加不了 HITable 的小组件** —— 这是系统层面的限制，不是应用故障。

### 自定义外观

「界面美化 → 桌面小组件配色」可换 6 套配色；
「界面美化 → 小组件背景」可给小组件铺一张自己的图片：

- **高斯模糊** 0~30，**明暗调整** 0~80%
- 开启后还可单独调节：**课程卡片透明度**、**文字内容透明度**（两者互不影响）
- 关掉开关即完全恢复默认样式

> 改完设置后，如果小组件没变化，删掉旧小组件重新添加一次即可。

---

## 界面美化（菜单 →「界面美化」）

- 6 套配色 + 自定义主色 / 强调色
- 深浅模式、AMOLED 纯黑
- 卡片圆角 4 档、紧凑度 3 档
- 卡片样式 3 种（纯色 / 色条 / 渐变）+ 不透明度可调
- **课表排布**：瀑布流 / 表格
- **主页背景**：预设图案，或选自己的图片（可调模糊度与明暗）
- **桌面小组件配色**：6 套预设 + 跟随应用
- **桌面小组件背景**：自定义图片 + 模糊 / 明暗 + 卡片 / 文字透明度
- **顶部问候语**：默认 `Hi，Table~`，可改任意文字、换字体（仿宋 / 楷体 / 黑体）、或隐藏

---

## 数据安全

- 完全离线运行，无账号、无云同步、无广告、无统计
- 课程与设置只保存在本机
- 菜单 →「备份与恢复」可导出 / 恢复 JSON 备份
- 换设备时用它迁移；卸载前请先导出备份

---

## 常见问题

**Q：提示「不在校园网」/ 读取失败？**
A：教务系统只在校内网可访问。校外需先连学校 VPN，或改用手工添加课程。

**Q：提醒不准时？**
A：国产系统会限制后台。按「提醒设置」页里的引导，关闭电池优化、允许自启动和后台活动。

**Q：桌面找不到小组件？**
A：见上文「添加方式」。华为 / 荣耀要下滑到「窗口小工具」；纯血鸿蒙不支持。

**Q：小组件背景图没铺满 / 显示不出来？**
A：请在「小组件背景」里确认已选择图片且开关打开；仍异常时删掉小组件重新添加。

**Q：导入后课程有冲突？**
A：重叠的课程会照常导入，方便你自行取舍。长按卡片可删除不需要的。

**Q：数据会丢吗？**
A：数据只存在本机。卸载前请先在「备份与恢复」里导出备份。

---

## 反馈

- QQ：647259208
